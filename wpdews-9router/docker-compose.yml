version: '3.8'

services:
  app:
    image: decolua/9router:latest
    restart: unless-stopped
    ports:
      - "20128:20128"
    volumes:
      - ${APP_DATA_DIR}/data:/app/data
    environment:
      - DATA_DIR=/app/data
      - HOSTNAME=0.0.0.0
      - PORT=20128
